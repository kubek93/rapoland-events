# test
# https://github.com/settings/developers
# https://docs.netlify.com/security/secure-access-to-sites/oauth-provider-tokens/#netlify-ui-settings
backend:
  name: github
  repo: kubek93/rapoland-events
  branch: main
  auth_scope: repo

media_folder: "public/images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "events"
    label: "Events"
    folder: "src/content/events"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    editor:
      preview: false
    fields:
      - { label: "Tytuł wydarzenia", name: "title", widget: "string" }
      - { label: "Opis wydarzenia", name: "description", widget: "text" }
      - { label: "Data startu", name: "start_date", widget: "datetime", format: "YYYY-MM-DD HH:mm", date_format: "YYYY-MM-DD", time_format: "HH:mm" }     - { label: "Lokalizacja", name: "location", widget: "string" }
      - { label: "Bilety", name: "tickets_url", widget: "string" }
      - { label: "Link do Google Maps", name: "google_maps_url", widget: "string" }
      - { label: "Zdjęcie", name: "thumbnail", widget: "image", required: true }
